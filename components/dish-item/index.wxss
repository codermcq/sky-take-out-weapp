:host {
  display: block;
}

.dish-item {
  display: flex;
  padding: 20rpx;
  background: #fff;
  border-radius: 16rpx;
  margin-bottom: 20rpx;
  overflow: hidden;
  box-sizing: border-box;
}

.dish-item.is-disabled {
  opacity: 0.5;
}

.dish-img {
  width: 160rpx;
  height: 160rpx;
  border-radius: 12rpx;
  flex-shrink: 0;
  background: #f5f5f5;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 60rpx;
}

.dish-info {
  flex: 1;
  margin-left: 20rpx;
  min-width: 0;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.dish-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.dish-desc {
  font-size: 24rpx;
  color: #999;
  margin: 6rpx 0;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.dish-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.dish-price {
  color: #FF6B35;
  font-size: 34rpx;
  font-weight: 700;
}

.dish-flavor-btn,
.dish-add-btn {
  padding: 6rpx 20rpx;
  background: #FF6B35;
  color: #fff;
  font-size: 22rpx;
  border-radius: 24rpx;
  line-height: 1.4;
}

.dish-add-btn {
  font-size: 28rpx;
  font-weight: 700;
  padding: 2rpx 18rpx;
}
